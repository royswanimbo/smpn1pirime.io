function updateModal(title, content) {
  document.getElementById("infoModalLabel").innerText = title;
  document.getElementById("modalContent").innerText = content;
}

// Tampilkan file yang diupload dalam bentuk link
// let preview = "";
// if (kk) preview += `<p>KK: <a href="#" target="_blank">${kk}</a></p>`;
// if (rapor) preview += `<p>Rapor: <a href="#" target="_blank">${rapor}</a></p>`;
// if (akta) preview += `<p>Akta: <a href="#" target="_blank">${akta}</a></p>`;
// localStorage.setItem("previewFiles", preview);
